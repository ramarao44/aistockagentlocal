# Human Intent: Update AI-DLC DOC Summary Generation Process

I want to update the AI-DLC documentation generation process so DOC produces one canonical PDF-style Markdown summary per request.

The process should add reusable output examples under `ai_dlc/docs/examples/`, document `ai_dlc/docs/summary/` as the generated summary destination, and update DOC guidance so duplicate summaries are not generated in multiple formats with the same content.

A governed Change Request will provide the detailed implementation scope and acceptance criteria.