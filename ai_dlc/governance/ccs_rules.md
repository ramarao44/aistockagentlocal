# CCS RULES

1. Any protected artifact change triggers CCS mandatory review.
2. CCS must verify lineage: PSC -> Human Intent -> FIS -> Specs -> Bolts -> Tests -> Runtime -> Docs.
3. Missing lineage is a hard block.
4. CCS outcome must be documented as PASS/BLOCK with evidence.
